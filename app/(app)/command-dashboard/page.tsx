import PagePlaceholder from "@/components/PagePlaceholder";

export default function CommandDashboardPage() {
  return (
    <PagePlaceholder
      title="Command Dashboard"
      description="Live deal pipeline, 5-phase acquisition workflow, capital stack automation, and agent workflows."
      phase={8}
    />
  );
}
