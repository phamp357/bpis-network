import PagePlaceholder from "@/components/PagePlaceholder";

export default function OraclePage() {
  return (
    <PagePlaceholder
      title="ORACLE"
      description="AI Client Activation Profile Generator — 7-question intake, readiness scoring, tier assignment."
      phase={4}
    />
  );
}
